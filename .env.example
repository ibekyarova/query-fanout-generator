# OpenAI API Key (required for real AI generation; without it, mock mode is used)
OPENAI_API_KEY=sk-...

# Optional: Redis URL for caching (future feature)
# REDIS_URL=redis://localhost:6379

# Optional: PostgreSQL for persistent history (future feature)
# DATABASE_URL=postgresql://user:password@localhost:5432/fanout
